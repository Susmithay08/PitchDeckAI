services:
  - type: web
    name: pitchdeck-frontend
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: VITE_API_URL
        value: https://pitchdeck-backend.onrender.com
